# Supabase Configuration
SUPABASE_PROJECT_URL=https://your-project-ref.supabase.co

# New API Keys (ES256 signing)
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...  # For client-side use
SUPABASE_SECRET_KEY=sb_secret_...            # For server-side use (KEEP SECRET!)

# Legacy Service Role Key (Required for supabase-py client compatibility)
# The Python Supabase client may not fully support the new sb_secret_* format yet
# If you encounter "Invalid API key" errors, add this key:
SUPABASE_SERVICE_ROLE_KEY=service_role_...   # Legacy format (KEEP SECRET!)

# Required for context summarization
GROQ_API_KEY=your_groq_api_key_here

# Required for article generation and web context extraction
MISTRAL_API_KEY=your_mistral_api_key_here

# Optional: Used for web searches if DuckDuckGo fails
SERPER_API_KEY=your_serper_api_key_here
